import React from 'react';

const AdminMin = ({el}) => {

     return (
        <div>
            <h1>{el.email}</h1>
            <h1>{el.password}</h1>
            
        </div>
    );
};

export default AdminMin;